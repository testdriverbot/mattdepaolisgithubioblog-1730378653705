version: 4.0.78
steps:
  - prompt: Locate the external GitHub link in the header.
    commands:
      - command: hover-image
        description: GitHub icon in the header
        action: hover
  - prompt: Click on the GitHub link.
    commands:
      - command: hover-image
        description: GitHub icon in the header
        action: click
  - prompt: Click on the GitHub link.
    commands:
      - command: hover-image
        description: GitHub icon in the header
        action: click
  - prompt: >-
      Assert that the browser opens the GitHub page in a new tab.  # Use the
      content of the file from the environment
    commands:
      - command: assert
        expect: the GitHub page is open in a new tab
  - prompt: >-
      Assert that the browser opens the GitHub page in a new tab.  # Use the
      content of the file from the environment
    commands:
      - command: assert
        expect: the GitHub page is open in a new tab
  - prompt: >-
      Assert that the browser opens the GitHub page in a new tab.  # Use the
      content of the file from the environment
    commands:
      - command: assert
        expect: the GitHub page is open in a new tab
